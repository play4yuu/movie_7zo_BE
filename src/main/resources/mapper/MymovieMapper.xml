<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.movie.mapper.MymovieMapper">
	<select id="movieSelect" resultType="com.campus.movie.vo.MovieVO">
		select movie_name, director, actor, runtime, show_date, genre,synopsis,poster_url,trailer_url,rating,reservationrate,audience_sum
		 from movie where movie_id=${param1}
	</select>
	
	<select id="getAllMovies" resultType="com.campus.movie.vo.MovieVO">
		select * from movie order by reservationrate desc limit 8
	</select>
	
</mapper>
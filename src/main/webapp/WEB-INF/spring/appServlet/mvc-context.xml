<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	<!-- 
		Interceptor는 접속하면 DispacherServlet의 매핑주소에 따라 실행이 인터셉트 된다.
		
		AOP: aspect oriented programing
		1.환경설정
		beans시작태그에 context,mvc를 추가해야한다.(밑에 Namespaces탭에서 추가 가능(Source옆))
		
		2.Interceptor클래스 생성해준다.com.multi.campus안에 LoginInterceptor클래스를 HandlerInterceptorAdapter를 상속해서 만든다.
	 -->
	 <!-- 
	 
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/users/logout"/>
			<mvc:mapping path="/board/write"/>
			<mvc:mapping path="/board/writeOk"/>
			<mvc:mapping path="/board/edit"/>
			<mvc:mapping path="/board/editOk"/>
			<mvc:mapping path="/board/delete"/>
			<mvc:mapping path="/board/write"/>
			
			<bean class="com.campus.movie.LoginInterceptor"></bean>
		</mvc:interceptor>
	</mvc:interceptors>
	 
	  -->
	
	<!-- 파일업로드 -->
	<!-- 
	<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver"></bean>
	
	
	 -->
	
</beans>
